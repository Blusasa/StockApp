import React from "react";
import { Icon } from '@rneui/themed';

const Ionicons = ({name}) => {
  return (
    <Icon
      name={name}
      size={30}
      type="ionicon"
    />
  );

};

export default Ionicons;